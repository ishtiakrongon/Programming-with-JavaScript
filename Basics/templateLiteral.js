// ES6 multi-line template literals

let multiLine = `
    Using ES6
    backticks,
    multi-line
    strings are
    possible!
`;

console.log(multiLine);

let firstName = `Md. Ishtiak Ahmed`;
let lastName = `Rongon`;

console.log(`${firstName} ${lastName}`);